---
/**
 * Experience Section
 * Metrics + expandable case studies
 * Based on PRD: scannable in 30 seconds
 */

import { EXPERIENCE } from '@/config/experience'
import { ExperienceCard } from '@/components/sections/experience-card'
import { cn } from '@/lib/utils'
---

<section id='experience' class='py-24 md:py-32'>
  <div class='container'>
    <h2 class='font-mono text-2xl md:text-3xl font-bold mb-12'>Experience</h2>

    {/* Metrics */}
    <div class='grid grid-cols-1 md:grid-cols-3 gap-8 mb-16'>
      {
        EXPERIENCE.metrics.map((metric, index) => (
          <div class={cn(' border-accent md:pl-8', index > 0 && 'md:border-l')}>
            <div class='font-mono text-4xl md:text-5xl font-bold mb-2'>
              {metric.value}
            </div>
            <div class='font-mono text-lg text-primary mb-2'>
              {metric.label}
            </div>
            <p class='text-muted text-sm'>{metric.description}</p>
          </div>
        ))
      }
    </div>

    {/* Impacts / Case Studies */}
    <div class='space-y-4'>
      {
        EXPERIENCE.impacts.map((impact, index) => (
          <ExperienceCard
            client:visible
            title={impact.title}
            company={impact.company}
            role={impact.role}
            description={impact.description}
            highlights={impact.highlights}
            tags={impact.tags}
            className={cn(
              index === EXPERIENCE.impacts.length - 1 && 'border-b',
            )}
          />
        ))
      }
    </div>
  </div>
</section>

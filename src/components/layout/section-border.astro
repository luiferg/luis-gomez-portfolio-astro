---
/**
 * Section Border Component
 * Horizontal divider between sections
 * Based on PRD: Section borders provide structure
 */

interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<div class:list={['w-full h-px bg-border/30', className]}></div>
